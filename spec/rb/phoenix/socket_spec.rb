require "spec_helper"

RSpec.describe Rb::Phoenix::Socket do
  it "has a version number" do
    expect(Rb::Phoenix::Socket::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
